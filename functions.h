#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <stdio.h>
#include <string.h>
#include "defines.h"

void printPrompt(int prompt);
int toString(char a[]);
int isDollarFormat(char input[]);
int isSingleDigit(char input[]);

#endif